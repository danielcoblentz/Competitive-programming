{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Build and Run C++",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "args": [],
            "cwd": "${fileDirname}",
            "preLaunchTask": "Build and Run C++ (g++-15)",
            "MIMode": "lldb"
        }
    ]
}
